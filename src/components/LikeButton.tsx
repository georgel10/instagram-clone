import React from 'react';
import { Icon } from 'react-native-elements';

const LikeButton = () => {
  return <Icon name="heart" type="ionicon" size={32} color="red" />;
};

export default LikeButton;
